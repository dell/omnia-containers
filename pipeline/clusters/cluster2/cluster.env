# Cluster 2 Configuration
# Connection details + dataset selection for this cluster.
# Sensitive credentials live alongside this file in credentials.yml.
CLUSTER_NAME="cluster2"
TARGET_IP=""
TARGET_USER=""
TARGET_PASS="${CLUSTER2_TARGET_PASS}"  # Resolved from GitLab CI/CD variable (masked)
DATASET="cluster2_config"
BASE_TC="tc03_minimal_hpc"
