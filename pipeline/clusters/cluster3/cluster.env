# Cluster 3 Configuration
# Connection details + dataset selection for this cluster.
# Sensitive credentials live alongside this file in credentials.yml.
CLUSTER_NAME="cluster3"
TARGET_IP=""
TARGET_USER=""
TARGET_PASS="${CLUSTER3_TARGET_PASS}"  # Resolved from GitLab CI/CD variable (masked)
DATASET="cluster3_config"
BASE_TC="tc04_k8s_multisubnet"
