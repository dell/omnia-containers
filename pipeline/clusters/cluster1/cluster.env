# Cluster 1 Configuration
# Connection details + dataset selection for this cluster.
# Sensitive credentials live alongside this file in credentials.yml.
CLUSTER_NAME="cluster1"
TARGET_IP=""
TARGET_USER="root"
TARGET_PASS="${CLUSTER1_TARGET_PASS}"  # Resolved from GitLab CI/CD variable (masked)
DATASET="cluster1_config"
BASE_TC="tc01_production_standard"
