apiVersion: batch/v1
kind: Job
metadata:
  name: k8s-example-job
spec:
  template:
    spec:
      containers:
      - name: nginx
        image: nginx:latest
        command: ["echo", "Hello from k8s!!"]
        imagePullPolicy: Always
      restartPolicy: Never
