- name: Converge
  hosts: oim
  become: true
  tasks:
    - name: Run omnia playbook inside omnia_core container on remote host
      command: >
        podman exec omnia_core /bin/bash -c
        'cd /omnia && ansible-playbook /omnia/omnia.yml -i /opt/omnia/inv'
      register: result

    - name: Show playbook output (stdout)
      debug:
        var: result.stdout

    - name: Show playbook errors (stderr)
      debug:
        var: result.stderr
